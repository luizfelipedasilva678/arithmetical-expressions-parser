{
  "name": "shunting-yard-algorithm",
  "version": "1.0.0",
  "description": "Shunting Yard algorithm implementation to convert infix expressions into posfix expressions",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.13.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "ts-node": "ts-node",
    "test": "node -r ts-node/register -r tsconfig-paths/register --test ./src/tests/*.test.ts"
  }
}
